<div class="container">
    <div class="row">
        <div class="col-2"></div>
        <div class="col-8 mt-5">
            {{ form_start(form) }}
            {{ form_widget(form) }}
            <button type="submit" class="btn btn-secondary mt-3">{{ button_text }}</button>
            {{ form_end(form) }}
        </div>
    </div>
</div>